/* liste di adiacenza:
	creo un array di liste */

#include <stdio.h>

struct elemento{
	int nodo;
	struct elemento *next;
};
typedef struct elemento elemento;
typedef elemento *list;
link A[n];
A[i]=malloc(sizeof(elemento));
A[i]->nodo=j;
A[i]->next=NULL;
a->next=malloc(sizeof(elemento));
a->nodo=j;
a->next=NULL;
